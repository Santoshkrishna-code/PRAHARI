# golangci-lint configuration inside shared/
run:
  timeout: 3m
  issues-exit-code: 1
  tests: true

linters:
  disable-all: true
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - ineffassign
    - typecheck
    - gosec
    - revive
    - gofmt
    - bodyclose
